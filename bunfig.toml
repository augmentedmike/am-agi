[test]
# Bun's exclude is post-discovery (doesn't prevent traversal).
# Pass explicit paths instead: bun test agent scripts
# This file only sets the timeout — scope is controlled by CLI args.
timeout = 60000
