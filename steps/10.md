# Step 10: Dispatcher and Polling

Two things that make the board come alive:

1. **Dispatcher** — watches the board, picks up cards, runs AM with the right column prompt per state
2. **Board polling** — UI refetches cards every 5 seconds so moves are always visible

Once these are in place, a card on the board drives all work automatically. The first card to go through will implement the new card panel UI itself.

---

## Lessons Learned

**Board dev server locks to port 4200**

`package.json` dev script kills any prior process on :4200 before starting:
```
"dev": "next dev --turbopack --port 4200"
```
The LaunchAgent plist uses `/bin/sh -c` with `lsof -ti tcp:4200 | xargs kill -9 2>/dev/null;` before the npm command so launchd never fights a zombie process.

**Ship crashes the dev server — restart it automatically**

`git merge --ff-only <branch>` rewrites `apps/board/` in the main worktree. Next.js dev server detects the mass file change and crashes. Fix: add a `stepRestartBoard` as the final step of `shipCard` in `agent/src/git/commit.ts` that spawns a detached `npm run dev` in `apps/board/` after cleanup.

**`merge-base` must use local `main`, not `origin/main`**

`stepSquash` uses `git merge-base HEAD main` (not `origin/main`). We only push on ship, so `origin/main` lags behind and produces a wrong base SHA, corrupting the squash.

---

## New Card Panel — Feature Spec

**Button:** `new card` (no brackets) in the top bar right, pink accent.

**Panel:** slides down from the top bar. Single auto-resize textarea (grows with content, no scrollbar). `create` button submits POST /api/cards, closes panel, clears textarea.

**Ship flip animation:** when a card transitions to the `shipped` column, play a flip animation:
1. Card flips face-down with a smooth CSS 3D ease (0.4s)
2. Back face shows a large 🎉 emoji centered, holds for 2 seconds
3. Card continues flipping forward (same direction, no reverse) back to face-up (0.4s)
4. A small ship icon (🚢) appears on the card permanently after the animation

The animation triggers via a state change detection in `BoardClient.tsx` — when a card's state changes to `shipped`, trigger the flip for that card id.

---

## Prompt

```
Read steps/10.md, apps/board/src/worker/prompts.ts, CLAUDE.md, and docs/TOOLS.md.

Do the following. Commit after each.

**1. Build `bin/dispatcher`**

A Bun script (`#!/usr/bin/env bun`) that watches the board and runs AM iterations on active cards.

- Runs a loop every 5 seconds
- Each cycle: GET /api/cards, filter to backlog/in-progress/in-review, pick highest-priority card per state
- For each card, check if worktree `../am-<cardId>` exists
- If not: `git worktree add ../am-<cardId> -b <cardId>`
- Write `work.md` to the worktree:

# Card: <title> (<id>)
State: <state>
Priority: <priority>

## Instructions
<column prompt for current state>

- Column prompts from `apps/board/src/worker/prompts.ts`:
  - backlog → BACKLOG_PROMPT
  - in-progress → IN_PROGRESS_PROMPT
  - in-review → IN_REVIEW_PROMPT
- Call `runIteration(worktreeDir)` from `agent/src/loop/index.ts`
- Log: `[dispatch] <cardId> <state>`
- On error: log and continue, never crash
- AM uses `board move <id> <state>` to transition — dispatcher never touches card state directly

**2. Add polling to the board UI**

In `apps/board/src/components/BoardClient.tsx`, add a `setInterval` that fetches `/api/cards` every 5 seconds and updates cards state. Keep the existing SSE — polling is the fallback.

**3. Lock the dev port and update package.json**

Set `"dev": "next dev --turbopack --port 4200"` in `apps/board/package.json`.

**4. Create the card**

After building and committing, create the card that will be worked through the board:

```sh
board create --title "New card panel: new card button on top bar, slide-down panel, auto-resize textarea, pink accent, ship flip animation" --priority high
```

**5. Create LaunchAgents via install.sh**

Create `launchagents/install.sh` in the repo root. Do NOT hardcode any paths — detect everything at runtime for the current user's machine.

The script must:
- Detect `npm`, `bun`, and `claude` via `which`
- Detect repo root from the script's own location (`$(cd "$(dirname "$0")/.." && pwd)`)
- Build a `LAUNCHD_PATH` that includes the bin dirs for all three
- Generate `~/Library/LaunchAgents/am.board.plist` and `~/Library/LaunchAgents/am.dispatcher.plist` by writing them directly (not copying templates)
- `am.board.plist`: `/bin/sh -c` that kills port 4200 then runs npm dev; RunAtLoad + KeepAlive; logs to `/tmp/am-board.log`
- `am.dispatcher.plist`: runs bun dispatcher; `BOARD_URL=http://localhost:4200`; RunAtLoad + KeepAlive; logs to `/tmp/am-dispatcher.log`
- Unload then load both agents after writing them
- `chmod +x launchagents/install.sh`

Run it to verify:
```sh
./launchagents/install.sh
launchctl list | grep "am\."
tail -5 /tmp/am-board.log
tail -5 /tmp/am-dispatcher.log
```

Definition of done:
- `bin/dispatcher` exists and runs
- Board UI polls every 5 seconds
- `apps/board/package.json` dev script uses port 4200
- Card created in backlog for the new card panel work
- `launchagents/install.sh` exists, is executable, detects paths dynamically, and loads both agents cleanly
- `bun test` passes
```
